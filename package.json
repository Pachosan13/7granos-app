{
  "name": "7granos-app",
  "private": true,
  "scripts": {
    "dev": "npm --prefix project run dev",
    "build": "npm --prefix project run build",
    "lint": "npm --prefix project run lint",
    "preview": "npm --prefix project run preview",
    "typecheck": "npm --prefix project run typecheck"
  },
  "engines": {
    "node": ">=18"
  }
}
